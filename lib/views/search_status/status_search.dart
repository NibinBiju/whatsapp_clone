import 'package:flutter/material.dart';

class StatusSearch extends StatelessWidget {
  const StatusSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
